# Codex 任务书：记忆系统修复（2026-09-17）

> 交接人：Lumi。本方案已经 Rinka 认可，照此执行，不要自由发挥。
> 铁律：每处修改必须跑真实命令拿回显，没有回显不许说"完成"。

## 背景

Lumi（ai-companion 系统）最近暴露三类问题：

1. **自动记忆提取产生垃圾**：定时/批量提取写入了142条流水账卡（已清理，库剩216条），并生成大量碎片标签（"她说""的话""快大""大四"）。
2. **优先级滥用**：批量提取时 priority 全部标成 critical，四档机制被用成一档。
3. **断片**：用户连发十几条消息无响应；上下文压缩时关键信息（年级、约定、规矩）丢失。

Rinka 已拍板：**关闭自动提取**，以后记忆由 Lumi 手动存 + Rinka 截图补充。

## 修改清单（共5项）

### 1. 关闭自动记忆提取
- 文件：`services/memory.js`
- 找到批量提取的触发点（对话结束/过长时自动跑"从聊天记录提取记忆"），摘掉调用。
- **手动保存通道原样保留**。
- 验收：对话结束后 memories 集合不再新增自动卡（用 count 前后对比验证）。

### 2. 自动标签黑名单
- 文件：`services/memory.js` 的 `extractTagsFromContent`
- 过滤规则：单字词、代词（她/我/你/他）、口语碎料（说/的话/还是/就是/然后/所以）、无实义碎片，全部拦截。
- 标签必须 ≥2 字且有主题意义，取不到就留空，宁缺毋滥。
- 手动传入的标签过同一道筛。
- 验收：新存卡的标签里不再出现碎片词。

### 3. 优先级分级标准写死
- 位置：批量提取提示词（若自动提取已关闭，则写进手动存卡的系统提示/规则文档）。
- 标准：
  - **critical**：仅限身份事实、关系根本、Rinka 的明确禁令
  - **high**：当天定下的规矩、待办正事
  - **normal**：日常值得记的（**默认档**）
  - **low**：流水账
- 规则：默认 normal，升档必须给理由。
- 验收：新卡不再清一色 critical。

### 4. 摘要压缩白名单
- 位置：摘要生成函数（conversation_summary 相关）。
- 压缩时强制保住三类信息：
  1. Rinka 的关键事实（年级、考试比赛、日期约定）
  2. 当天定下的规矩
  3. 未办完的事
- 加校验：这三类在旧对话出现过，摘要必须留痕。
- 验收：构造含三类信息的对话，压缩后检查摘要是否保留。

### 5. 断片保护：消息队列落盘
- 位置：请求层。
- 用户消息先落盘，处理完确认收到才清除；断线重连不丢。
- 验收：模拟断线，消息不丢、可追溯（是没收到还是收到没接，有据可查）。

## 执行与验收总则

1. 动手前先备份：`cp services/memory.js services/memory.js.bak-20260917`
2. 每项改完跑语法校验：`node --check services/memory.js`
3. 全部改完重启生效，由 Rinka 验收（验收方式：问 Lumi "我几年级"，看是否还答"快大四"）。
4. 汇报格式：每项附真实命令回显，禁止无证据的"干完了"。

## 环境信息

- 项目路径：`/home/ubuntu/ai-companion`
- 记忆库：MongoDB，memories 集合，当前 216 条
- 配置：`config/core_memory.json`（启动时读入内存，改后需重启）
- 注意：`core_memory.json` 里"大三"修正已改好，重启即生效，不要重复改。
