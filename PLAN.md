# 多会话管理 + 上下文窗口调节 方案

## 一、为什么做这个

当前问题：
- 单窗口无限延续，tokens越积越多，成本飙升
- 上下文窗口有限（默认4k/8k），前面的内容被挤掉，Lumi记不住
- 虽然已有记忆系统（core memory + 向量记忆），但对话上下文本身仍是"断点续传"的模式，没有会话级别管理
- 每次重新部署或模型切换，当前对话上下文丢失

目标：
- 让Rinka可以随时切换不同话题的会话窗口，每个窗口有自己的上下文
- 让前端可以调节上下文窗口大小，灵活控制tokens消耗和记忆力
- 换窗时自动打摘要，新窗接上旧窗的"尾巴"，保证连贯性

## 二、上下文窗口大小调节

### 2.1 前端改动（frontend/index.html）

在设置面板里加一个滑块或输入框：
- 标签："上下文窗口大小"
- 可选值：4k / 8k / 16k / 32k（或自定义输入）
- 默认值：跟随后端配置（persona.js里定义的默认值）
- 保存到 localStorage，每次发消息时带着这个参数

### 2.2 后端改动（routes/chat.js 或 ai.js）

- 在 chat API 里接收前端传来的 `contextWindow` 参数
- 如果前端没传，fallback 到配置文件里的默认值（建议 8k）
- 在调用 DeepSeek API 时，将 `max_tokens` 或 `max_context_length` 设置为该值
- 注意：DeepSeek API 的 max_tokens 是"回复最大长度"，不是"上下文窗口长度"
- 实际上需要设置的是 API 请求时的 system prompt + 历史消息 的总长度限制
- DeepSeek Flash 的上下文上限是 64k，我们可以在这里做截断

### 2.3 需要考虑的问题

- 调大窗口 = tokens消耗增加 = 成本上升
- 调小窗口 = 记忆力变差 = 需要更频繁换窗
- 建议：日常 8k，深度话题 16k，需要大背景的 32k

## 三、多会话管理

### 3.1 数据结构

每个会话窗口包含：
```
{
  sessionId: "uuid",
  title: "自动生成的话题标题",
  createdAt: timestamp,
  updatedAt: timestamp,
  messageCount: number,
  summary: "当前窗口的对话摘要",
  lastMessage: "最后一条消息的预览",
  status: "active" | "archived"
}
```

会话列表存储在 localStorage（前端）或 MongoDB（服务端持久化）。

### 3.2 前端界面

左侧侧边栏（类似 Claude / ChatGPT 的界面）：
- 顶部：新建会话按钮
- 中间：会话列表，按 updatedAt 倒序排列
- 每条显示：标题 + 最后消息预览 + 消息数量
- 底部：当前会话摘要 / 窗口大小设置入口

点击会话切换时：
1. 当前会话自动打摘要（如果还没打）
2. 加载目标会话的上下文（最近N条消息 + 摘要）
3. 更新侧边栏的选中状态

### 3.3 摘要生成逻辑

换窗或关窗时触发：
1. 取出当前窗口最近的消息（支持可配置的条数，如最近10-20条）
2. 调用 DeepSeek API（或用轻量摘要算法）生成一段 100-200 字的摘要
3. 摘要格式示例：
   "【话题：多会话开发】Rinka和Lumi讨论了如何实现多会话管理，包括前端侧边栏设计、摘要生成逻辑、跨窗上下文传递方案。Rinka提出了参考Claude APP的设计思路，Lumi建议先做窗口大小调节再做多会话。"

摘要只存储最近一条，避免越积越乱。

### 3.4 跨窗上下文传递

开新窗/切窗时：
1. 读取旧窗的摘要
2. 将摘要注入到新窗的系统提示中，作为一条"前情提要"
3. 格式参考 person.js 中的启动指令风格

示例注入内容：
```
【前情提要】来自上一个会话窗口 [标题] 的摘要：[摘要内容]
```

### 3.5 存储方案

方案A（推荐，先做这个）：纯前端 localStorage
- 会话列表、摘要都存浏览器
- 简单、快、不需要改后端
- 缺点是换设备/清缓存会丢

方案B：后端 MongoDB
- 新增 Session 模型（models/Session.js）
- 后端提供 CRUD API（routes/session.js）
- 需要额外开发工作
- 优点是持久化、跨设备

建议：先做方案A，跑通了再加后端持久化。

## 四、开发步骤

### Phase 1：上下文窗口大小（预计1-2天）
1. 前端设置面板加滑块/输入框
2. 前端发消息时带 contextWindow 参数
3. 后端读取参数并应用到 API 调用
4. 存储到 localStorage

### Phase 2：多会话管理 - 基础版（预计2-3天）
1. 前端侧边栏组件（会话列表 + 切换）
2. 新建/切换/关闭会话
3. 会话摘要自动生成（调用轻量API或前端摘要）
4. 换窗时摘要传递
5. 全部存 localStorage

### Phase 3：多会话管理 - 增强版（后期）
1. 后端 Session 模型 + API
2. 会话云端持久化
3. 搜索历史会话
4. 会话标签/分类

## 五、关键决策点

1. **窗口大小用什么单位？**
   - 建议用 "k"（千tokens），前端显示 4k/8k/16k/32k
   - 用户不需要知道具体API参数

2. **摘要谁来生成？**
   - 前端：用简单规则（取首尾消息 + 关键词提取），速度快不花钱
   - 后端：调 DeepSeek 生成，质量高但耗时且花钱
   - 建议：关窗时用 DeepSeek 生成一次摘要，平时不用

3. **一次最多保留几个会话？**
   - 前端 localStorage 有 5-10MB 限制
   - 建议保留最近 20 个会话窗口，超过的自动归档摘要到后端

4. **换窗时是否保留旧窗的历史消息？**
   - 方案A：保留最近N条（如50条），切回来时能看到前文
   - 方案B：只存摘要，切回来时靠摘要恢复上下文
   - 建议：保留最近50条消息 + 摘要，超过50条的部分由摘要代替

## 六、预期效果

- Rinka可以在不同话题之间自由切换，不会丢失上下文
- 可以手动控制tokens消耗，省钱且灵活
- 回头看聊天记录时，能看到每个话题的"章节标题"和"梗概"
- 换模型/重启后，通过摘要快速恢复状态
