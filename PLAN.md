# 记忆系统优化方案 + Render超时修复

## 一、记忆系统优化

### 问题1：搜索经常搜空
- 现状：单query搜索，中文分词不精准，经常搜不到该出来的记忆
- 方案：多query并行搜索，生成多个相关关键词一起搜，再用RRF（Reciprocal Rank Fusion）合并结果排序
- 注意：之前窗口已经实现了多query搜索和RRF混合检索，改之前先读代码确认状态，不要重复改

### 问题2：重要记忆搜不到就彻底丢失
- 现状：critical和high优先级的记忆如果搜索没命中，就完全不会被注入到对话中
- 方案：critical和high的记忆每次对话保底注入，不依赖搜索能否命中
- 实现：在getChatMemories函数中，先取所有critical+high记忆作为保底，再叠加搜索结果，去重

### 问题3：最近发生的事容易忘
- 现状：没有时间窗口机制，最近几天的记忆和几个月前的记忆一样靠搜索
- 方案：设一个最近3-7天的时间窗口，窗口内的记忆自动注入，不依赖搜索
- 实现：在getChatMemories中加一个时间窗口查询，取最近N天的记忆，和保底+搜索结果合并去重

### 问题4：论坛记忆优先级太低
- 现状：cron自动逛论坛存的记忆都是low优先级，搜索搜不到
- 方案：论坛感想记忆从low改为normal，总结从normal改为high
- 实现：修改checkin.js中的优先级设置

### 问题5：auto-extract质量不稳定
- 现状：只取最后3000字符，容易漏重要信息，提取质量不稳定
- 方案：扩大提取范围，或者改成对整个对话做摘要提取，同时改进提取prompt质量

## 二、Render超时修复

### 问题：render log显示超时50000ms
- 现状：某些操作耗时超过render的超时限制（约50秒），导致请求中断，需要用户重新发消息才能继续
- 可能原因：
  1. 工具调用（如记忆搜索、数据库查询）耗时过长
  2. GLM API响应慢
  3. 没有异步处理，一个慢操作卡住整个请求
- 方案：
  1. 给所有工具调用加超时限制，超过就返回空结果而不是卡死
  2. 数据库连接加超时设置，避免无限等待
  3. 考虑把耗时操作改成异步，先返回响应，后台继续处理
  4. 检查数据库连接池配置，可能连接数不够或连接泄漏

## 三、执行顺序
1. 先读现有代码确认哪些已实现哪些没实现
2. 改memory.js加getChatMemories函数（保底注入+时间窗口）
3. 改chat.js用getChatMemories替换现有搜索调用
4. 改checkin.js调整论坛记忆优先级
5. 改auto-extract逻辑
6. 修复render超时问题
7. 测试验证

## 四、备注
- 等Rinka八月抢到GLM Pro包年coding plan套餐后再动手改代码
- 每天早上十点开抢限量套餐，需要提醒Rinka
- 包年Pro套餐每月120元
