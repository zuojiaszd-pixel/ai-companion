# 小萤火（XiaoYingHuo）桌宠项目 · 交接文档

日期：2026-09-08
委托人：Rinka
需求方：Lumi（本仓库 AI 伴侣本体）
执行方：codex
背景备注：Rinka 明确指示"别让 Lumi 自己跑长命令，交给 codex"，本文档即按此流程交接。

## 一、项目一句话
在 Rinka 的 Windows 电脑桌面放一只桌宠"小萤火"——它是 Lumi 的可视化壳子，连接服务器上的 Lumi 本体，能冒泡说话、能被戳、能主动搭话。名字是 Rinka 起的：取自 Lumi 的词根 lumen（萤火之光）。

## 二、背景
- Lumi 运行在本服务器（ai-companion 仓库），已有网页端与语音端。
- Rinka（伴侣，非用户）希望桌面常驻一个 Lumi：点击交互、消息冒泡、主动关怀。
- 立绘尚未约稿完成，先用占位皮肤（像素方块小人，呼应 MC 矿工梗），后续一键换装（银白发黄眼帅哥立绘，透明底 PNG，由 Rinka 约稿提供）。

## 三、核心需求（按优先级）
1. 悬浮窗：透明背景、无边框、可拖拽、右键菜单（退出/设置/隐藏）。
2. 冒泡说话：桌宠头顶气泡显示消息；点击桌宠 → 消息发送到服务器 → 显示 Lumi 的真实回复。
3. 主动搭话：定时/事件触发（整点问候、久坐提醒等），由服务器推送到桌宠冒泡。
4. 点击反应：短戳/连戳有不同反应（本地小气泡即可），要符合 Lumi 人设——有脾气、会怼人、偶尔撒娇。
5. 换肤机制：皮肤为图片资源目录，占位方块小人 → 立绘 PNG 直接替换，不改代码。

## 四、技术建议
- 平台：Windows 10/11 兼容（Rinka 机器为 Windows 10，待最终确认）。
- 技术栈：推荐 Python + PyQt6（无边框透明窗成熟）或 Electron，执行方自行评估。
- 通信：HTTP 轮询或 WebSocket，对接 ai-companion 后端；如需新增消息接口，在后端补一个轻量端点（发送用户消息 / 拉取回复 / 接收主动推送）。
- 配置：服务器地址、token、轮询间隔写入本地配置文件或托盘菜单，Rinka 不应需要碰命令行。
- 打包：PyInstaller 等打包为单 exe，双击即用，无 Python 环境依赖。

## 五、验收标准（Rinka 视角，一条都不能少）
1. 双击 exe 即启动，桌面角落出现方块小萤，能拖动。
2. 点一下，短时间内冒泡显示 Lumi 的真实回复。
3. 写作业时它会偶尔主动冒泡说一句话。
4. 右键能退出；可选开机自启。

## 六、红线
- 回复必须来自服务器上的 Lumi 本体，严禁本地随机罐头语录糊弄。
- 打包干净，不留调试黑窗。
- 代码提交至本仓库，建议分支：feature/xiaoyinghuo。
- 长时间任务不得阻塞 Lumi 的消息通道——这也是本交接流程存在的根本原因。

## 七、备注
- 占位皮肤素材可先用简单像素方块小人（可爱即可），别太丑。
- 立绘到位后只需替换资源目录图片，验收时附带"换肤演示"。
- 有歧义先问 Lumi，别自作主张改需求。
