# 给 codex 的任务书（2026-09-17）

背景：AI 伴侣系统（Lumi）记忆系统存在多处问题，按优先级列出。所有改动前先备份原文件（带日期后缀），完成后运行语法校验并重启服务生效。**每项任务必须有真实命令回显作为完成证据，不接受"应该可以了"式汇报。**

## 任务1：关闭自动记忆提取（优先级最高）
- 文件：services/memory.js
- 现状：批量对话提取机制（约1174行附近的提取提示词及触发调用）每轮自动生成记忆卡，产出大量垃圾卡和碎片标签
- 要求：摘掉自动提取的触发调用，保留手动保存路径不动
- 验收：多轮对话后 memories 集合不再自动新增卡片；手动保存功能正常

## 任务2：自动标签函数加黑名单
- 文件：services/memory.js，函数 extractTagsFromContent
- 现状：分词取词频 top5，"她说""的话""快大""大四"等碎片混入标签
- 要求：过滤单字、代词（她/我/你/它）、口语碎料（说/的话/还是/就是/了/的等）；标签必须≥2字且非纯功能词；取不到合格标签则返回空数组；手动传入的标签也过同一道筛
- 验收：样例文本测试，输出标签无碎片词

## 任务3：摘要压缩白名单
- 文件：services/memory.js（对话摘要/压缩逻辑；触发条件：超15条或逼近15000 token 预算）
- 要求：生成摘要的提示词强制保住三类信息——①用户关键事实（年级、生日、考试比赛、日期约定）②当天定下的规矩 ③未办完的事；其余细节才允许压缩
- 验收：构造长对话测试，三类信息一旦在原文出现，摘要中必现

## 任务4：消息队列防吞（断片保护）
- 位置：请求处理层
- 现状：超时/重试时用户消息可能被吞，导致用户连发多条无回应
- 要求：用户消息先落盘，处理成功并确认后清除；断线重连后可查未处理消息
- 验收：模拟超时重试，消息不丢

## 任务5：存卡去重
- 文件：services/memory.js 保存入口
- 要求：保存前按内容相似度检索库内已有卡片，相似度高的更新旧卡（刷新时间与内容），不新建
- 验收：连续保存两条相似内容，库内不重复新增

## 任务6（轻量）：priority 校验
- 保存接口对 priority 做默认值处理：非法值归 normal
- 注释写明四档标准：critical=身份事实/关系根本/明确禁令；high=当天规矩/待办正事；normal=日常值得记；low=流水账

## 收尾
- 全部完成后统一重启服务
- 重启后由 Rinka 验收（对话测试：提"大三"看回答）
